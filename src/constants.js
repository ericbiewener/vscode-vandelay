const SUPPORTED_LANGS = ['js', 'py']
const MultilineImportStyle = {
  MULTI: 'multi',
  SINGLE: 'single',
}
const LineModifier = {
  BEFORE: -1,
  SAME: 0,
  AFTER: 1,
}
module.exports = {
  MultilineImportStyle,
  LineModifier,
  SUPPORTED_LANGS,
}
