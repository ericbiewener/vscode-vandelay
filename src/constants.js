const SUPPORTED_LANGS = ['JS', 'PY']
const MULTILINE_IMPORT_STYLE = {
  multi: 'multi',
  single: 'single',
}
const LINE_MODIFIER = {
  before: -1,
  same: 0,
  after: 1,
}
const INDEX_JS = 'index.js'

module.exports = {
  MULTILINE_IMPORT_STYLE,
  LINE_MODIFIER,
  INDEX_JS,
  SUPPORTED_LANGS,
}
